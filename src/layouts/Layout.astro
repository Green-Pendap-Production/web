---
import "../styles/global.css";
import Sidebar from "../components/Sidebar.astro";

interface Props {
	title?: string;
	description?: string;
}

const {
	title = "Green Pendap Production",
	description = "Professional Game Development Studio",
} = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<meta name="description" content={description} />
		<title>{title}</title>
	</head>
	<body class="flex min-h-screen bg-gpp-bg text-gpp-text">
		<Sidebar />
		<!-- Main Content Area with left margin to accommodate fixed sidebar -->
		<main class="grow ml-64 p-12 min-h-screen max-w-5xl">
			<slot />
		</main>
	</body>
</html>
